{
  "name": "phased-gates",
  "note": "Phase D's non-suite gate: the MCP check battery (via the tools/ shim, per the kube entry contract). The suite itself runs as suite-312.json at the same ref; byte-identity rides the suite's own frozen-fingerprint pins.",
  "resources": {
    "requests": { "cpu": "2", "memory": "6Gi" },
    "limits": { "cpu": "2", "memory": "8Gi" }
  },
  "arms": [
    {
      "id": "mcp-battery",
      "entry": "tools/mcp-check.mjs",
      "args": [],
      "env": { "SIMLIB_REALM": "main" }
    }
  ],
  "nodeSelector": { "karpenter.sh/nodepool": "ephemeral-pool" },
  "tolerations": [
    { "key": "gasboat.ephemeral", "operator": "Equal", "value": "true", "effect": "NoSchedule" }
  ]
}
