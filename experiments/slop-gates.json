{
 "name": "slop-gates",
 "note": "The slop landing's targeted gate + the dish economics. Focus arms run the foodways scenario family by name filter on both backends (mutation demos EXPECT a red arm - read receipts, not Job status). The measure arms are the foodways close-out's own instrument (8 towns x 20 days, stock vs learned) repointed at slop - the bun's numbers were pigs 73->109, spent 2132->4186, share flat ~0.36; slop's equivalents are REPORTED, not tuned.",
 "resources": { "requests": { "cpu": "2", "memory": "6Gi" }, "limits": { "cpu": "2", "memory": "8Gi" } },
 "arms": [
  { "id": "foodways-js", "entry": "tools/suite.mjs", "args": ["foodways:"], "env": { "SIMLIB_REALM": "main" } },
  { "id": "foodways-wasm", "entry": "tools/suite.mjs", "args": ["foodways:"], "env": { "SIMLIB_REALM": "main", "SIMLIB_KERNEL": "wasm" } },
  { "id": "voice-pins-js", "entry": "tools/suite.mjs", "args": ["voice"], "env": { "SIMLIB_REALM": "main" } },
  { "id": "measure-stock", "entry": "tools/measure-buns.mjs", "args": ["stock"], "env": { "SIMLIB_REALM": "main" } },
  { "id": "measure-learned", "entry": "tools/measure-buns.mjs", "args": ["learned"], "env": { "SIMLIB_REALM": "main" } }
 ]
}
