{
  "name": "rep-focus",
  "note": "The reputation pass's own scenarios (name filter 'rep:'), both backends - the cheap targeted gate for green checks and mutation demos. Full suite + phased-gates still run on the final SHA.",
  "resources": {
    "requests": { "cpu": "2", "memory": "6Gi" },
    "limits": { "cpu": "2", "memory": "8Gi" }
  },
  "arms": [
    {
      "id": "js-rep",
      "entry": "tools/suite.mjs",
      "args": ["rep:"],
      "env": { "SIMLIB_REALM": "main" }
    },
    {
      "id": "wasm-rep",
      "entry": "tools/suite.mjs",
      "args": ["rep:"],
      "env": { "SIMLIB_REALM": "main", "SIMLIB_KERNEL": "wasm" }
    }
  ],
  "nodeSelector": { "karpenter.sh/nodepool": "ephemeral-pool" },
  "tolerations": [
    { "key": "gasboat.ephemeral", "operator": "Equal", "value": "true", "effect": "NoSchedule" }
  ]
}
