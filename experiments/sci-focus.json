{
  "name": "sci-focus",
  "note": "The title-credit occlusion scenario alone, both backends. Used for its mutation demo: pin the credit block back to a fixed PANEL_Y offset and this must go RED naming the overlap, or the scenario is decoration and the bug returns a third time.",
  "resources": {
    "requests": { "cpu": "2", "memory": "4Gi" },
    "limits": { "cpu": "2", "memory": "6Gi" }
  },
  "nodeSelector": { "karpenter.sh/nodepool": "ephemeral-pool" },
  "tolerations": [
    { "key": "gasboat.ephemeral", "operator": "Equal", "value": "true", "effect": "NoSchedule" }
  ],
  "arms": [
    { "id": "sci-js", "entry": "tools/suite.mjs", "args": ["the title's credit block never lands on the science button"], "env": { "SIMLIB_REALM": "main" } },
    { "id": "sci-wasm", "entry": "tools/suite.mjs", "args": ["the title's credit block never lands on the science button"], "env": { "SIMLIB_REALM": "main", "SIMLIB_KERNEL": "wasm" } }
  ]
}
