# Rulings, 2026-08-24

Recorded by the orchestrator from Matt's own selections in the live session.
Forks do not record operator rulings; if a ruling appears in a branch doc
without a pointer to this file or to Matt's own words, distrust it.

## 1. The depart-rule weight inflation — MEASURE FIRST

> **Matt:** "Measure first, then rule"

The ~52,000x inflation (`44 + 20 * r.hunger` written for hunger 0..1, now
reading Q20 integers) is NOT ruled on yet. Before deciding whether the
float-era band comments or the current arithmetic express the intent, run a
**departure-card census** on the cluster: across 48 towns x 30 days, count
which rule wins each card, today versus under restored bands. Decide from the
distribution.

Consequences:
- **E3 is unblocked and lands as transcribed.** Its contract is equality with
  the engine, and faithfully reproducing a pre-existing bug is correct for a
  transcription slice. The census is a separate follow-up against the ENGINE,
  not against E3.
- The census is a new task, not part of E3's ceremony.

## 2 + 3. The economy trio — LAND ALL THREE, MATRIX IMMEDIATELY AFTER

> **Matt, on reputation:** "Hold for the combination matrix"
> **Matt, on the trio:** "Land all three, measure after"

**These two selections conflict on ordering** and the conflict is recorded
here rather than resolved silently. The reading taken:

- Reputation does **not** land alone — that is what "hold for the combination
  matrix" protects against, and it is the substance of the first answer.
- The trio (visitor-stats, reputation, interruptible-commitment) lands
  **together**, and the combination matrix runs **immediately after** — that
  is the second answer's ordering.
- **Pre-commitment:** the matrix result is acted on, not filed. If combined
  growth lands far above the intended difficulty, the trio gets adjusted or
  partially unpicked. Landing first is only defensible if the measurement
  that follows is binding.

Known solo numbers (each measured, each gated green alone):

| change | growth escapes |
| --- | --- |
| base | 13/48 |
| visitor-stats | 32/48 |
| reputation | 17/48 |
| interruptible-commitment | 17/48 |
| **all three** | **UNMEASURED** |

The risk being accepted, stated plainly: with three changes landing at once,
a bad combined number is hard to attribute. CLAUDE.md already records the
erosion pattern this invites — four individually-defensible passes once took
a pillar from 5/16 to 1/16.

Reputation lands with its branch's proposed values: arrival gates pig 80->45,
gull 60->34, REP_SPILL 25%.

## 4. E4's staff ladder — RUNGS, WITH 6 KEPT

> **Matt:** "Keep 6 as a rung, add higher ones"

`HEAD_CAP.steps = [4, 6, 8, 12]`. A town starts at 6 (today's behaviour) and a
vote moves it one rung, which is what turns the cap from a wall into politics.

Separately and regardless of E4: the campaign platform refusing more than 6
staff, and its window text running off the screen, are UI defects. They are
not blocked on this ruling.

## Still unruled

- **Body slice-2 declarations** and the possible Sigma-floor (~80).
- **Slop economics acceptance** — the shack's margin now floats with the live
  fish price.
- **Depart weights: value-level check or behavioural equivalence?** Tonight's
  finding (design/cs35-research/redgate-bisect-2fe4ec4.md and the
  crab-depart.json header): the sweep proves behavioural equivalence, and a
  weight typo too small to flip an argmax is invisible. Measured: 36000->36001
  ships 12/12 green; 36000->360 reds at row 456. Whether executable rules
  deserve a stronger contract than "same decisions" is open.
