CROSS-ENGINE RECEIPT - numeric slice 1a (money -> integer cents)
protocol numeric-protocol.md par.9: the converted subsystem's fields must be
bit-identical under a second JS engine.

engine A: V8 / node v26.5.1 (the suite's own pins, tools/suite.mjs)
engine B: JavaScriptCore (macOS 26.6 system framework), same two game days,
          same seeds, sandbox mirroring tools/simlib.mjs

seed 1337: BIT-IDENTICAL to the V8 pin
seed 4242: BIT-IDENTICAL to the V8 pin

Every money field (coins, SUDSY's till, all seven wallets) is an integer
count of cents and matches across engines exactly. rep, serves, catch and
rage match too; positions still match because they are float-but-simple
today - slice 4 is what makes THAT a guarantee rather than a coincidence.

Harness: design/cs35-research/numeric-wip/xengine.js (jsc dialect: no vm
module, so the game files evaluate in one Function body with the sandbox
destructured in; drive through window.rafCb - a destructured copy never
sees the game's own requestAnimationFrame assignment).
