# CLOSE-OUT: THE CITIZEN MIND (dream-replay rung 1, with rung 0 inside it)

The crew make up their own minds. `cit_errand.candidate` — pickErrand's whole
off-counter census — is the second decision surface behind the policy layer,
distilled from the script teacher, shipped shadow-proven then LIVE, carrying
the delta-ready per-actor format (364-byte `dm` + `dr` cursor slots in the
save envelope, zero-filled this era) that rung 3's dreaming will write into.

## The surface (and one census correction)

THIRTEEN classes, not the design doc's sketched twelve: the take() census
turned up `selfcook:drink` (the barkeep's own pour at a dark bar). The census
is the authority; the doc's list was a sketch. Candidates and their draws stay
script; the brain ranks CLASSES; within the winning class the script's own
exact-rational score picks the instance (nearest tap, nearest ballot table —
the two-post idiom stays geometry's, not the net's). A candidate the script
refuses outright (DETOUR_MAX's n<0) is no candidate for the brain either.

## THE ENGINE-OWNED REGIME (the live flip's real lesson)

The first live suite run failed 14 scenarios, and the failures were a MAP OF
WHAT WAS NEVER UP FOR LEARNING. The brain replaced errandScore, and with it
the DIRE desperation fast-path and the nudge preference — so SUDSY went back
to 41% of her life on the dehydration line, the drop nudge (the player's
thumb!) was ignored, and a starving sick crab was offered a wash. The fix is
the design doc's own principle applied to citizens: **clamps stay engine-
owned.** `citEngineOwned(c, cand)` routes to the script whenever (a) any
candidate's need is at DIRE, (b) the player's nudge matches a candidate, or
(c) the crab is SICK — illness is the mortality ladder, measured to the
percent, and life-support is not a personality. The brain governs the
ordinary day. Both scorers are draw-free, so the lockstep receipt holds
whoever decides. Rails healed 7 of the 10 behavioral reds outright
(dehydration week, nudge, sick-day, staff-meal pricing x2, M/E fatigue
levelness, rivalry-offer).

## The artifact (h48-x4-s11) and the pin that shaped it

39 inputs (the `citizen.*`/`cit.*` registry block — all existing integer
reads) → 48 hidden → 13 classes, R1=6. Corpus: 215,012 thinks, 48 lever-
diverse towns × 14 days (stage v3), brains disarmed, 0.0454 thinks/tick.
Trained at the sim's own class prior (noneRatio null per the v3 ruling) with
thin classes ×4 loss weight — the OTHER half of the lever, exactly what the
distillery's docs reserve it for: `none` is 94.4% of a citizen's corpus and
ball:fun 0.5%.

- Held-out (12 towns): **97.96%**, act-early 379, act-late 553, EVERY
  supported class ≥ 42.9% recall (vote 96.3%, soup 74.6%, selfcook:food
  77.6%, ball:fun 42.9%). In-town shadow: **99.82%** (seed 4242, 1,125 thinks).
- **The pin refused the top scorers**: w1 (98.23%) and thin-x2 (98.39%)
  candidates had ball:fun at 0.9–24.8% — a dead class behind a better
  headline, the refused-seed-7 lesson again. The shipped brain scores lower
  and PLAYS BALL.
- Ball was unlearnable until the vector grew `citizen.ball.dist.px` and the
  job flags — the teacher's gate reads the detour and the staff privilege,
  and an observable the net cannot see is a lever that breaks in play
  (teacher-coverage rule, third confirmation).
- **The wasm xcheck leg caught a real contract bug**: `citizen.shift.leave.rel`
  leaked a fraction (leaveGmin carries walk-time minutes; 714.666 reached the
  corpus). Floored at the registry door; the collector now refuses any
  non-integer observable loudly. Final receipt: logits + choices
  **bit-identical across node/V8 scalar, wasm, and JavaScriptCore**.

## Rung 0, shipped inside: the delta format

`classifyD(fv, dm)`: L = base*256 + (w2d·h + b2d), exact in doubles (< 2^37);
zero/absent delta reproduces the backbone argmax bit-for-bit (scenario over
400 town-assembled vectors, logits proven 256×). SAVE_VER 3 → 4: personas may
carry `dm` (int8 w / int32 b, range-checked in saveProblem with the offending
number named) and `dr` (u32 dream cursor). The LOAD door strips an
arch-mismatched delta LOUDLY with both shapes named; a ver-3 envelope loads
as the zero delta (scenario). Nobody writes a delta this era.

## The ceremony

- First crossing: seed 1337, think 397, T=7606 — DRIFT holds off on a shack
  drink the script buys. Streams PAIRED to the crossing (no draw leak). Seed
  4242 crosses nowhere in two days; its fingerprint pin stands untouched.
- Re-pins (both receipted in place): frozen day-2 fingerprint (1337 only),
  rng day-2 draw count 1737 → 1616 (day 1 holds at 1726 — the held-off drink
  spends no draw of its own; the trajectory reshapes day 2's custom).
- Re-stagings, each walked by mechanism: the vis shadow-inert scenario's
  `BRAINS = {}` leg now deletes only the vis policy (the bundle ships a live
  citizen brain; the old leg measured the wrong thing); both citizen shadow
  scenarios force mode=shadow explicitly; the taps "always reachable" probe
  asks the CANDIDATE BOARD with the decider disarmed (offered ≠ picked; the
  dehydration-week pillar gates what waiting may cost, un-restaged); the sale
  fixture holds REEF's purse through settlement (her brain-era books bought
  the fixture out from under the BUY-chip test — her queue priority keeps its
  own scenario); the election re-staged 909 → 7 with the sweep documented.
- **Suite 312/312 BOTH backends; MCP 49/49.** Shadow commit (3418e84) was
  byte-identical to base by headless diff before the flip.
- Mutations, all four bit: srand in shadowCitObserve → inertness names the
  books; **zeroed w2 → the ACTED FLOOR** (a constant-none lobotomy BEAT the
  plain 90% agreement floor at 97%+ — this surface's prior is the trap; the
  scenario now demands ≥50% agreement on the thinks the teacher acted on);
  loosened int8 clamp → door names the 200; +3 in the zero path → the
  256× receipt names the logit.

## The matrix (same instrument, same seeds, live vs mode-off)

- Baseline: **0/48 and 0/48** — intact, medians 13/13/13 vs 12/13/13.
- Growth: **LIVE 19/48 (3/9/7) vs SCRIPT 15/48 (4/4/7)** — the citizen mind
  GROWS the town: +4 escapes, sb16 median eviction 12 → 31. Nothing was
  tuned; the delta is reported per the §7 ruling (a changelog, not a gate),
  and it is positive. Mechanism, honestly guessed not proven: brain crabs
  hold off on marginal purchases (the act-late direction), keeping wallets
  for fuller service runs.
- REPORTED behavioral shift: **brain-era voter turnout is lower** (election
  tallies 5–7 papers → 3–4 across the fixture sweep). Temperament now decides
  whether a free crab walks to the box — the doc's own "where you are and
  when you are free" plus who she is. The mechanism is intact (ballots,
  count, declaration); phase E civics is where the franchise gets levers if
  Matt wants turnout back.

## Settlers (the tip moved under this branch)

A settled pig resident dispatches through `BRAINS[c.p.culture]` — no pig
citizen policy exists, so pig citizens run the script by construction, zero
special cases. The seam is open: a pigway that declares `cit_errand.candidate`
gets thinking settlers the day it ships one. Crab-only this rung, by design.

## Inspector

`brainOf` now answers for residents: select a crew crab, press `t` (or the
MIND> chip beside SEND — SEND owns x4–34, the chip takes x36) and THE MIND OF
PINCHY renders with the same meter bank (13 rows; the frame grows 7px per
class over the visitor's 7, formula reproduces the visitor panel's exact
pixels), BECAUSE saliency through the citizen registry labels, PAST strip.
Verified headless: live thinks captured (PINCHY, ring 16, mode live).

## Honest gaps

- ball:fun recall 42.9% is the floor of the shipped table — alive, not sharp.
  More ball-adjacent observables (game-in-progress age, court occupancy)
  are the known next lever if play shows too few pickup games.
- The "thinking heads round-trip" scenario reads SAVE_KEY (the legacy key)
  for its envelope and may be vacuous since the slots era — pre-existing,
  untouched, flagged here.
- tools/neuro/cit-distill-v1.mjs is the shipped recipe end-to-end (collect →
  weight ladder → pin table → winner); cit-data.json (37MB) stays local like
  the vis corpus; cit-data-meta.json + cit-seed-table.json are committed.
